install all the packages ----------------------------------
run all the files from ipynb -----------------------------
new 2 pickle files will be created, create new model folder, move those two pickle files to the model folder. -------------------------
then run app.py using `streamlit run app.py`
